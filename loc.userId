const fs=require('fs'); let c=fs.readFileSync('server/routes.ts','utf8'); c=c.split('locations = allLocations.filter(loc = === sessionUser?.username);').join('locations = allLocations.filter(loc = === sessionUser?.username 
